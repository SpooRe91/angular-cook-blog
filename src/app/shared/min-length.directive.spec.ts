import { MinLengthDirective } from './min-length.directive';

describe('MinLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new MinLengthDirective();
    expect(directive).toBeTruthy();
  });
});
